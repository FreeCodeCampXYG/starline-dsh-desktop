# DEV_STATE

截至 2026-08-16，`v0.5.0` 已正式发布：annotated tag 对象 `8f32315` 指向版本提交 `70c805a`，该提交的 CI `31928652053` 已通过质量门和 Windows、macOS、Linux 六个平台原生构建，Release 工作流 `31929012111` 已通过六平台打包与发布。公开 Release 为稳定版、非草稿、非预发布，共 33 个资产：16 个主包、16 个独立 SHA-256 和 `SHA256SUMS.txt`；其中 Ubuntu 24.04 x64/ARM64 在线 DEB 分别为 3,599,286 与 3,242,198 字节，发布正文已固定写入 Windows 10 22H2/11、macOS 13.5+、Ubuntu Desktop 24.04（Linux 6.8 GA、glibc 2.39、GTK3、WebKitGTK 4.1）、Node 22.19+ 或 24+ 及包内 Node 24.19.0 要求。项目继续固定 Go 1.25、Wails v2.14.0、`@deepseek-ai/dsh@0.1.0-rc.6`；`offline-full` 保持独立归档，不把数万个 Node 依赖装入 DEB，旧系统和其他 Linux 发行版不受支持。核心发布文件为 `release-requirements.json`、`scripts/package-linux-deb.sh`、`.github/workflows/ci.yml`、`.github/workflows/release.yml`、`scripts/generate-release-notes.mjs` 与 `CHANGELOG.md`。按用户约束未在本机编译、安装依赖、制作或下载发行包；本轮本地只完成版本/JSON/YAML/Shell/PowerShell/Node、Release notes 单测、Markdown 链接和 Git diff 静态检查。首轮 DEB 预演因 Bash 展开未转义的 `~` 产生非法版本，已改用字面量 `\~` 并在线复验通过。当前未完成项是 Windows/macOS 签名、公证及代表性 Ubuntu 设备的 GUI、应用菜单、首次启动、升级和卸载体验验证；后续修复必须发布递增版本，不能移动 `v0.5.0`。

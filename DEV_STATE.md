# DEV_STATE

截至 2026-08-16，项目已在保持 Go 1.25、Wails v2.14.0、固定 `@deepseek-ai/dsh@0.1.0-rc.6` 与六平台在线/离线分发的基础上，为 Ubuntu Desktop 24.04 LTS 增加 x64/ARM64 在线 DEB，并让未来每个 tag 的 Release 页面从版本化要求文件固定记录该版本的系统要求。`scripts/package-linux-deb.sh`、CI/Release 矩阵、DEB 资产及 SHA-256 规则、Release notes 生成器、Issue 表单、README 和维护文档均已更新；DEB 安装到 `/usr/bin` 并提供应用菜单、图标、许可证，依赖从 `release-requirements.json` 读取并固定为 glibc 2.39+、`libgtk-3-0t64`、WebKitGTK 4.1 和 CA 证书，Node/npm 因可能由 nvm/Volta 等用户级工具提供而保持建议项，应用仍执行真实版本检查。支持边界固定为 Windows 10 22H2/11、macOS 13.5+、Ubuntu Desktop 24.04（Linux 6.8 GA 基线），旧系统和其他 Linux 发行版不受支持；`offline-full` 继续使用 TAR.GZ，避免把数万个 Node 依赖装进 DEB。核心文件为 `release-requirements.json`、`scripts/package-linux-deb.sh`、`.github/workflows/ci.yml`、`.github/workflows/release.yml`、`scripts/generate-release-notes.mjs` 与 `docs/SYSTEM_REQUIREMENTS.md`。按用户约束未在本机编译、安装依赖、制作或下载 DEB，仅完成 Git diff、JSON/YAML、Shell 语法、Release notes 单测和 15 个 Markdown 链接的静态检查；首轮线上运行因 Bash 把未转义 `~` 展开为 runner home 而产生非法 DEB Version，修复为字面量 `\~` 后，提交 `35efa5b` 的 CI 运行 `31927510392` 已通过质量门和六平台原生构建，手动 `0.5.0-dev` Release 预演 `31927517803` 已通过六平台打包且按设计跳过公开发布。x64/ARM64 DEB 均确认 control 版本 `0.5.0~dev`、架构分别为 `amd64`/`arm64`、依赖字段正确，并完成 ELF/动态库检查和 apt 安装/卸载；Actions artifacts `starline-dsh-desktop-linux-x64` 与 `starline-dsh-desktop-linux-arm64` 已上传并保留至 2026-08-30。公开 v0.4.0 仍是 14 个主包、29 个总资产且不含 DEB；尚未完成代表性 Ubuntu 设备 GUI/菜单、升级和卸载体验验证，也未创建新 tag，下一步需在精确确认递增版本并补齐对应 CHANGELOG 段后发布，不能移动历史 tag。

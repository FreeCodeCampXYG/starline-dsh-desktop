# DEV_STATE

更新时间：2026-08-20。公开基线已推进到 `v0.6.1`：提交 `55b8110` 已推送 `main`，annotated tag `v0.6.1` 已推送并回读远端 ref；随后仅格式化修复提交 `96d733c` 已推送 `main`，未移动已公开 tag。`internal/launcher/environment.go` 为 npm 单次请求设置 10 秒等待和 1 次重试，无代理时优先使用 `registry.npmmirror.com`，继承或自定义代理不会在失败时偷偷绕过；`internal/updater/dsh.go` 对版本检查使用受信任 registry、有限连接/响应超时和无代理镜像优先；`internal/config/settings.go`、`frontend/src/main.ts` 和 `internal/application/app.go` 增加 30–600 秒可配置的在线本地 DSH 页面启动等待上限（旧配置零值默认 90 秒），并在更新后的运行时下载或页面指纹校验失败时尝试恢复更新前的精确版本与配置，同时保护并发配置冲突；`README.md`、`CHANGELOG.md`、`docs/ARCHITECTURE.md`、`docs/TROUBLESHOOTING.md` 和 `docs/KNOWN_ISSUES.md` 已注明镜像/宿主超时只负责 npm 准备与本地 Harness Web 就绪，不改变 DeepSeek Harness 内部模型/API 请求策略。已完成 `git diff --check`、JSON/版本元数据静态检查和线上反馈的 gofmt 根因定位；本机未运行 Go/TypeScript 编译、Go 测试、npm 安装或跨平台构建。下一步是回读 `main` 的新 CI 结果；若需要让格式化修复进入发布资产，必须使用新的递增 tag，不能移动 `v0.6.1`。

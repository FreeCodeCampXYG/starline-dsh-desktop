# DEV_STATE

截至 2026-08-16，项目继续以“Go 1.25 + Wails v2.14.0 薄桌面宿主启动固定 `@deepseek-ai/dsh@0.1.0-rc.6`”为目标，本轮 v0.3.3 交付候选包含：审计 `anywhere-labs/deepseek-harness-desktop` 的公开 Issue 后，`main.go` 已为正式 WebView 启用默认右键菜单，给跨源 DSH 页面提供手工选择/复制/粘贴回退；`scripts/verify-offline-runtime.mjs` 已把 Sharp/Koffi 从仅加载提升为真实 PNG 转换、平台动态库加载和进程 ID 函数调用，Windows 还加载 `ole32.dll`，并继续执行 ripgrep 与真实 PTY；`frontend/src/main.ts` 明确 DSH `danger-full-access` 可按当前用户权限执行更广泛的 PowerShell/文件操作，但不会自动启用、不会获得 UAC 管理员令牌；README、CHANGELOG、架构/构建/发布/排错/已知问题/开源参考文档已区分 v0.3.2 已发布证据与 v0.3.3 交付候选，Wails、前端、离线运行时、Windows 构建脚本和 Release 手动输入版本已统一到 v0.3.3。关键决策仍是启动真实 Node 和完整 npm 依赖树、保留独立 DSH 子进程/loopback/日志边界，不移植 Electron `process.execPath` 补丁、不用 `SSH_CONNECTION=1` 伪装原生目录选择、不复制透明自绘标题栏，也不在沙箱失败时静默降级到 `danger-full-access`；Windows ACL 沙箱内部的黑框与 `0xC0000142`、MCP/profile/plugin readiness 失败仍属于可能穿透外壳的 DSH 上游问题。本机 Windows x64 已用官方 SHA-256 校验的临时 Go 1.25.0 完成 `gofmt`、`go test ./...` 和 `go vet ./...`，并通过前端 `npm ci`、文档链接检查（14 个 Markdown）、TypeScript 检查、Vite 构建、发布说明测试（1/1）、Windows 离线 Sharp/Koffi/ripgrep/PTY 功能复测、严格 Wails 审计（26 pass、0 warn、0 fail）和 `git diff --check`；v0.3.2 的既有六平台 CI/Release 只能证明旧基线，新改动尚未经过六平台原生 CI、最终归档和代表性设备安装验证。下一步应先在新提交上运行六平台 CI 与非发布打包演练，再从 Windows ARM64、Intel/Apple Silicon Mac 和 Linux x64/ARM64 设备收集右键/剪贴板、工作区选择、PowerShell 权限模式、全屏关闭、图标安全区、首次启动、升级与卸载结果；在此之前不得宣称跨平台 GUI、签名/公证或 DSH 上游沙箱问题已解决。

# DEV_STATE

截至 2026-08-16，项目继续以 Go 1.25、Wails v2.14.0 薄宿主和固定 `@deepseek-ai/dsh` 为可复现基线；v0.3.3 已在提交 `e8c4972` 发布 29 个公开资产，提交 `f35a0b3` 则完成了不破坏 `offline-full` 原生依赖闭包的 DSH 手动更新通道。已确认 npm 官方 `latest`/`next` 仍为项目内置的 `0.1.0-rc.6`，因此没有升级锁文件；当前代码包含 `internal/dshversion` 的 SemVer 校验比较、`internal/updater` 的 npm 官方元数据检查、用户配置中的精确 DSH 版本、在线包“检查/应用/恢复默认”界面、离线包与环境变量覆盖保护，以及仅提醒不自动合并的 `/offline-runtime` Dependabot 规则，并已同步 README、架构、发布、排错和变更日志。关键决策是默认版本继续由 Desktop Release 固定，检查只在用户点击时发生且不下载依赖，应用时由后端再次核对官方 `latest`，前端不能指定任意包或版本；在线更新失败可回退，离线更新必须发布新的原生验证包。按用户要求没有在本机编译、安装依赖或重建运行时，只执行了 `gofmt`、YAML/文档链接/差异等静态检查；GitHub Actions 运行 `31904096389` 已确认质量门中的前端检查与构建、Go 格式/test/vet、离线依赖闭包，以及 Windows/Linux/macOS x64/ARM64 六平台 Wails 构建和相应离线运行时功能测试全部成功。已知缺口仍包括代表性设备安装/升级验证、Windows/macOS 签名公证，以及未来上游 DSH 新版本可能带来的 Node 要求、生命周期脚本或原生依赖变化；下一步应等待官方版本实际更新后审查 Dependabot PR 和锁文件变化，再通过相同线上门禁发布包含新固定运行时的版本，不在用户端或本机无审查地自动升级。

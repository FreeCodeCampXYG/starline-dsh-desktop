# DEV_STATE

更新时间：2026-08-17

当前目标是以 `v0.5.2` 发布 DSH 在线更新闭环：应用启动后通过用户已选择的 inherit/custom/disabled 代理模式自动查询 npm 官方 `latest` 与 `next`，在顶栏和设置中同时告知版本；自动检查只提示，在线包由用户确认通道后再次核对 dist-tag、保存精确 SemVer、同步回收本应用持有的旧 DSH 子进程树并重启，`offline-full` 仍固定已验证的 `@deepseek-ai/dsh@0.1.0-rc.6`，不在用户电脑上原地替换原生依赖。核心实现位于 `internal/updater/dsh.go`、`internal/application/app.go`、`internal/launcher/runtime.go` 和 `frontend/src/main.ts`：更新器改用固定的 npm DSH dist-tags 端点，限制响应、拒绝非官方重定向、发送 no-cache 请求并在结束后关闭 HTTP 空闲连接；后端只接受 `latest`/`next`，前端不能提交任意包名或版本；在线 npx 启动已移除会命中陈旧 packument 的强制 `--prefer-offline`，继续使用 npm 默认元数据校验和内容缓存。对应 Go 回归测试覆盖 latest/next、缺少 next、自定义代理、通道白名单和 npx 参数；README、架构、排错、已知问题、发布文档、CHANGELOG 及全部 `0.5.2` 版本元数据已同步。已确认 npm 官方 dist-tags 端点通过 `127.0.0.1:1080` 返回 `latest=0.1.0-rc.7`、`next=0.1.0-rc.7`；`git diff --check` 与五个 JSON 文件解析通过，离线依赖仍为 rc.6。按项目约束未在本机编译、安装依赖或运行 Go/TypeScript 测试；本机缺少 gofmt/Go 和前端 `node_modules`，一次只读 TypeScript 解析尝试因此无法执行，未安装任何内容。下一步按已授权的 Starline Git 门禁提交并通过 `10808` 推送 `main` 与 annotated tag `v0.5.2`，只确认 GitHub Release 工作流已触发；六平台编译、原生依赖、归档与 DEB 结果以线上日志为准。仍缺 Windows/macOS 签名、公证及代表性设备上的更新体验验证。

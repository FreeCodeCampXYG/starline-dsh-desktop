# DEV_STATE

截至 2026-08-17，当前目标是把 Agent 遗漏 `dsh plugin --profile` 的兼容修复发布为 `v0.5.1`，且不移动已公开的 `v0.5.0`。本机固定的 `@deepseek-ai/dsh@0.1.0-rc.6` 产物和官方 `master` 源码均确认：PowerShell 已正常启动，失败来自 DSH CLI 要求 `dsh plugin --profile <name> ...`，不是 Shell 权限。提交 `299578c` 已在 `main` 实现进程级兼容入口：`internal/launcher/dsh_command_shim.go` 创建临时 `dsh`/`dsh.cmd`/`dsh.ps1` 与内嵌 Node 转发器，`launcher.go` 只把该目录前置到当前 DSH 子进程环境；仅在 `plugin` 完全遗漏 `--profile` 时补为当前 `web` profile，显式 profile 和其他 DSH 命令原样转发，DSH 退出后删除临时目录，不修改全局 PATH 或 npm 文件。`dsh_command_shim_test.go` 覆盖默认补全、显式参数保持和其他命令保持；GitHub Actions 运行 `32023493664` 已确认质量门、Go 格式/测试/vet、前端与离线依赖闭包，以及 Windows x64/ARM64、macOS Intel/Apple Silicon、Linux x64/ARM64 的原生 Shell 测试和应用构建全部成功，其中 Windows 同时验证 PowerShell 与 CMD。版本元数据、CHANGELOG、README、构建/发布/排错及已知问题文档已准备为 `v0.5.1`；按长期约束未在本机编译、安装依赖或运行 Go 测试，发布前仍需让版本提交通过线上 CI，再创建递增 annotated tag 并由 Release 工作流完成六平台最终归档。已知边界是插件管理仍依赖官方要求的 pnpm、网络或缓存，该入口不提升 PowerShell/DSH 权限；当前公开 `v0.5.0` 不含本次修复。

# DEV_STATE

截至 2026-08-16，项目继续以 Go 1.25、Wails v2.14.0 薄宿主和固定 `@deepseek-ai/dsh` 为可复现基线；v0.3.3 已在提交 `e8c4972` 完成质量门禁、Windows/Linux/macOS x64/ARM64 六平台 CI、29 个公开资产和 Release 发布，当前开发目标是在不破坏 `offline-full` 原生依赖闭包的前提下增加 DSH 手动更新。已确认 npm 官方 `latest`/`next` 仍为项目内置的 `0.1.0-rc.6`，因此没有升级锁文件；本轮新增 `internal/dshversion` 的 SemVer 校验比较、`internal/updater` 的 npm 官方元数据检查、用户配置中的精确 DSH 版本、在线包“检查/应用/恢复默认”界面、离线包与环境变量覆盖保护，以及仅提醒不自动合并的 `/offline-runtime` Dependabot 规则，并同步 README、架构、发布、排错和变更日志。关键决策是默认版本继续由 Desktop Release 固定，检查只在用户点击时发生且不下载依赖，应用时由后端再次核对官方 `latest`，前端不能指定任意包或版本；在线更新失败可回退，离线更新必须发布新的原生验证包。按用户要求本轮禁止本机编译和依赖安装，目前只允许执行 `gofmt`、YAML/文本/差异等静态检查；新增 Go 测试及前端类型检查和构建门禁尚未实际运行，必须提交到 GitHub 后由 Actions 完成六平台编译、测试和离线运行时门禁。已知缺口仍包括代表性设备安装/升级验证、Windows/macOS 签名公证，以及上游新 DSH 版本可能带来的 Node 要求、生命周期脚本或原生依赖变化；下一步是完成静态审阅与文档检查，推送 `main` 触发线上 CI，失败时只依据 Actions 日志做最小修复，不在本机重建运行时。

# DEV_STATE

截至 2026-08-16，项目以 Go 1.25、Wails v2.14.0 薄宿主和固定 `@deepseek-ai/dsh@0.1.0-rc.6` 为可复现基线，当前目标是发布 v0.4.0；v0.3.3 已在提交 `e8c4972` 发布 29 个公开资产，提交 `f35a0b3` 完成了 DSH 手动更新通道，版本提交 `0a7e768` 已把 CHANGELOG、README、Wails、前端/离线 npm 清单、Release workflow、Windows 构建脚本和构建文档同步到 0.4.0。已确认 npm 官方 `latest`/`next` 仍为 `0.1.0-rc.6`，因此 v0.4.0 不升级离线锁文件；当前代码通过 `internal/dshversion` 和 `internal/updater` 实现 npm 官方元数据检查与 SemVer 校验，在线包由用户确认后保存精确版本并可恢复默认，离线包及环境变量覆盖受到保护，Dependabot 只提醒受审查升级。按用户要求没有在本机编译、安装依赖或重建运行时，只执行了 YAML、JSON、文档链接和差异等静态检查；GitHub Actions 运行 `31905089754` 已确认最终版本提交的前端检查与构建、Go 格式/test/vet、发布说明测试、离线依赖闭包，以及 Windows/Linux/macOS x64/ARM64 六平台 Wails 构建和相应离线运行时功能测试全部成功。已知缺口仍包括 v0.4.0 标签构建与公开资产尚未完成、代表性设备安装/升级验证、Windows/macOS 签名公证，以及未来上游 DSH 变化可能带来的 Node、生命周期脚本或原生依赖风险；下一步是创建并推送 annotated tag `v0.4.0`，等待 Release workflow 重新完成六平台打包，再核对 Release notes、29 个预期公开资产及校验清单。

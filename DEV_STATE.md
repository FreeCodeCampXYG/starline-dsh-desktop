# DEV_STATE

更新时间：2026-08-20。当前公开基线仍为 `v0.6.0`，本地未提交改动的目标是收紧普通包的在线运行时网络边界并让 `latest`/`next` 切换可回退：`internal/launcher/environment.go` 为 npm 单次请求设置 10 秒等待和 1 次重试，无代理时优先使用 `registry.npmmirror.com`，继承或自定义代理不会在失败时偷偷绕过；`internal/updater/dsh.go` 对版本检查使用受信任 registry、有限连接/响应超时和无代理镜像优先；`internal/config/settings.go`、`frontend/src/main.ts` 和 `internal/application/app.go` 增加 30–600 秒可配置的在线本地 DSH 页面启动等待上限（旧配置零值默认 90 秒），并在更新后的运行时下载或页面指纹校验失败时尝试恢复更新前的精确版本与配置，同时保护并发配置冲突；`README.md`、`CHANGELOG.md`、`docs/ARCHITECTURE.md`、`docs/TROUBLESHOOTING.md` 和 `docs/KNOWN_ISSUES.md` 已注明镜像/宿主超时只负责 npm 准备与本地 Harness Web 就绪，不改变 DeepSeek Harness 内部模型/API 请求策略。已完成 `git diff --check`；因本机没有 Go/gofmt，且项目约束禁止本地依赖编译、Go 测试和 npm 安装，本轮未运行这些命令。关键未决项是线上 CI 的 Go/前端构建、六平台原生运行时与更新回退验证，以及确认 DSH 上游接口请求是否提供独立的可配置超时；完成前不要移动公开 tag 或推送当前改动。

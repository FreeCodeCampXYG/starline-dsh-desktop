# DEV_STATE

截至 2026-08-16，项目正在准备 `v0.5.0` 正式发布：应用、前端、离线运行时和 Windows 构建脚本版本已统一为 `0.5.0`，CHANGELOG 已建立对应版本段，README 与维护文档已改为 Ubuntu Desktop 24.04 x64/ARM64 在线 DEB 和便携 TAR.GZ 的当前分发说明。项目继续固定 Go 1.25、Wails v2.14.0、`@deepseek-ai/dsh@0.1.0-rc.6`，支持边界为 Windows 10 22H2/11、macOS 13.5+、Ubuntu Desktop 24.04（Linux 6.8 GA、glibc 2.39、GTK3、WebKitGTK 4.1），旧系统和其他 Linux 发行版不受支持；`offline-full` 保持独立归档，不把数万个 Node 依赖装入 DEB。核心发布文件为 `release-requirements.json`、`scripts/package-linux-deb.sh`、`.github/workflows/ci.yml`、`.github/workflows/release.yml`、`scripts/generate-release-notes.mjs` 与 `CHANGELOG.md`。按用户约束未在本机编译、安装依赖、制作或下载发行包；本轮 `git diff --check`、六个 JSON 文件解析、全部 GitHub YAML 解析、Shell/PowerShell/Release notes 脚本语法、Release notes 单测及 15 个 Markdown 文件链接检查均通过。既有线上证据为提交 `35efa5b` 的 CI `31927510392` 和手动 `0.5.0-dev` Release 预演 `31927517803`，六平台原生构建与打包均通过，两个 DEB 已确认 control 版本 `0.5.0~dev`、amd64/arm64 架构、依赖、ELF、动态库及 apt 安装/卸载。首轮 DEB 预演因 Bash 展开未转义的 `~` 产生非法版本，已改用字面量 `\~` 并在线复验通过。当前尚未创建 `v0.5.0` tag 或正式 Release，下一步是提交并等待该提交的线上 CI 成功，再创建不可移动的 annotated tag，由 tag workflow 生成预期 16 个主资产、16 个独立校验文件和 `SHA256SUMS.txt`；代表性 Ubuntu 设备 GUI/菜单、首次启动、升级和卸载体验仍缺验证。

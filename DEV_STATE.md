# DEV_STATE

截至 2026-08-16，项目目标是在保持 Go 1.25、Wails v2.14.0、固定 `@deepseek-ai/dsh@0.1.0-rc.6` 与现有六平台在线/离线分发的基础上，为 Ubuntu Desktop 24.04 LTS 增加 x64/ARM64 在线 DEB，并让每个 tag 的 Release 页面固定记录该版本的系统要求。已完成 `scripts/package-linux-deb.sh`、CI/Release 矩阵、DEB 资产与 SHA-256 规则、Release notes 生成器、Issue 表单、README 和维护文档改造；DEB 安装到 `/usr/bin` 并提供应用菜单、图标、许可证，依赖从 `release-requirements.json` 读取并固定为 glibc 2.39+、`libgtk-3-0t64`、WebKitGTK 4.1 和 CA 证书，Node/npm 因可能由 nvm/Volta 等用户级工具提供而保持建议项，应用仍负责真实版本检查。系统支持边界定为 Windows 10 22H2/11、macOS 13.5+、Ubuntu Desktop 24.04（Linux 6.8 GA 基线），旧系统和其他 Linux 发行版不受支持；`offline-full` 继续使用 TAR.GZ，避免把数万个 Node 依赖装进 DEB。核心文件为 `release-requirements.json`、`scripts/package-linux-deb.sh`、`.github/workflows/ci.yml`、`.github/workflows/release.yml`、`scripts/generate-release-notes.mjs` 与 `docs/SYSTEM_REQUIREMENTS.md`。按用户约束未在本机编译、安装依赖或制作 DEB，仅确认 Git diff 无空白错误、JSON/YAML 可解析、Shell 语法通过、Release notes 单测通过、15 个 Markdown 文档链接通过；实际 x64/ARM64 构建、DEB control/ELF/动态库检查、apt 安装/卸载和 Release 预演仍待 GitHub Actions 验证，验证前不能宣称 DEB 已可下载或完成设备兼容测试；公开 v0.4.0 仍是 14 个主包、29 个总资产且不含 DEB，后续若创建公开 tag 必须使用递增版本并补齐对应 CHANGELOG 段，不能移动历史 tag。

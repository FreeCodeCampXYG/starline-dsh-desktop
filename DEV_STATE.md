# DEV_STATE

更新时间：2026-08-17

当前目标已定版为 `v0.6.0`：为 DSH 启动和通道切换增加可验证的阶段百分比，并把 `offline-full` 固定基线升级到 `@deepseek-ai/dsh@0.1.0-rc.7`。`internal/launcher/launcher.go` 按运行时检测、Node 校验、环境组装、子进程启动、监听地址和 HTTP 页面指纹报告 30%–98% 的单调里程碑，`internal/application/app.go` 使用 generation 隔离旧进度并在就绪时置为 100%，前端显示百分比和阶段；该值不是 npm 下载字节，在线 npx 停在 65% 表示仍在解析或准备依赖。离线清单和 lockfile 已一致固定 rc.7，并同步 `node-pty@1.2.0-beta.15` 的精确完整性、生命周期命令、脚本 SHA-256 以及 Windows ConPTY、macOS helper、Linux prebuild 结构门禁；真实 PTY、Sharp、Koffi、ripgrep 和最终归档仍由六平台 Release 工作流验证。Windows NSIS 显式允许覆盖，隔离安装测试会修改已安装二进制后重跑同一 Setup，以验证复用 `InstallLocation` 并替换程序文件；用户配置和 DSH 工作区位于安装目录外。`offline-full` 仍是便携归档而非安装器，升级应关闭旧程序、解压到新目录并验证后再移除旧目录，不能覆盖运行中的依赖树。已通过 `git diff --check`、15 个 Markdown 链接、JSON/YAML 解析、JavaScript 语法、rc.7 lockfile/白名单一致性及临时进程/目录回收检查；一次 `--prefer-offline` 锁解析因陈旧元数据误报 `ETARGET`，一次在线解析超时且遗留的本任务 npm 进程已回收，最终纯离线 `package-lock-only` 使用已缓存元数据成功。按约束未运行 Go/TypeScript 编译、Go 测试、`npm ci`、NSIS 安装测试或离线运行时构建；本机缺少 Go/gofmt，跨平台和覆盖升级证据必须由 `v0.6.0` 的线上 CI/Release 获得。Windows/macOS 签名、公证及代表性设备升级验证仍未完成；后续修复必须使用递增版本，不移动公开 tag。

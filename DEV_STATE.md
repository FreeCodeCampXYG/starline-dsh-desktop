# DEV_STATE

截至 2026-08-16，项目以 Go 1.25、Wails v2.14.0 薄宿主和固定 `@deepseek-ai/dsh@0.1.0-rc.6` 为可复现基线，v0.4.0 已发布；提交 `f35a0b3` 完成 DSH 手动更新通道，版本提交 `0a7e768` 同步 CHANGELOG、README、Wails、前端/离线 npm 清单、Release workflow、Windows 构建脚本和构建文档，annotated tag `v0.4.0` 指向含线上验证记录的提交 `0905308`。已确认 npm 官方 `latest`/`next` 仍为 `0.1.0-rc.6`，所以该版本没有升级离线锁文件；当前代码通过 `internal/dshversion` 和 `internal/updater` 实现 npm 官方元数据检查与 SemVer 校验，在线包由用户确认后保存精确版本并可恢复默认，离线包及环境变量覆盖受到保护，Dependabot 只提醒受审查升级。按用户要求没有在本机编译、安装依赖、重建运行时或下载大型发布资产，只执行了 YAML、JSON、文档链接、差异和 GitHub API 元数据检查；GitHub Actions 运行 `31905089754` 已确认最终版本提交的质量门与六平台原生构建，Release 运行 `31905639939` 又完成六平台打包、最终离线归档功能复测和发布，公开 Release 已核对为非草稿稳定版，包含 14 个非空主包、14 个独立 SHA-256 与 `SHA256SUMS.txt` 共 29 个资产，发布正文含版本变更、分平台下载表和未签名提示。已知缺口仍包括代表性设备安装/升级验证、Windows/macOS 签名公证，以及未来上游 DSH 变化可能带来的 Node、生命周期脚本或原生依赖风险；后续应保持 tag 不可变，若发现发布缺陷使用递增版本修复，并在官方 DSH 实际更新后审查 Dependabot PR 与锁文件变化再重复六平台门禁。

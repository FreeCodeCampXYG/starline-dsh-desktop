# DEV_STATE

截至 2026-08-17，当前目标是修复 Agent 在 Starline 启动的 DSH Web 中执行 `dsh plugin --help` 或 `dsh plugin add --help` 时反复报缺少 `--profile`。本机固定的 `@deepseek-ai/dsh@0.1.0-rc.6` 产物和官方 `master` 源码均确认：PowerShell 已正常启动，失败来自 DSH CLI 要求 `dsh plugin --profile <name> ...`，不是 Shell 权限。实现采用进程级兼容入口：`internal/launcher/dsh_command_shim.go` 创建临时 `dsh`/`dsh.cmd`/`dsh.ps1` 与内嵌 Node 转发器，`launcher.go` 只把该目录前置到当前 DSH 子进程环境；仅在 `plugin` 完全遗漏 `--profile` 时补为当前 `web` profile，显式 profile 和其他 DSH 命令原样转发，DSH 退出后删除临时目录，不修改全局 PATH 或 npm 文件。`dsh_command_shim_test.go` 覆盖默认补全、显式参数保持、其他命令保持，并要求 Windows PowerShell/CMD 与 Unix Shell 的结果一致；CI 已增加 Linux 质量门及六个平台原生 runner 的 launcher 测试，CHANGELOG、README、架构和排错文档已同步。按长期约束未在本机编译、安装依赖或运行 Go 测试；Node 语法、GitHub YAML、15 个 Markdown 文件链接、`git diff --check` 与 Wails node-web-host 严格审计 26/26 均通过。本机未安装或发现 `gofmt`，因此 Go 格式与功能测试仍待 GitHub Actions 权威验证；插件管理仍依赖官方要求的 pnpm、网络或缓存，该兼容入口不提升 PowerShell/DSH 权限。下一步是提交 `main`、等待六平台 CI，失败时只修复已证实的问题；本次未请求新 tag，不能移动已发布的 `v0.5.0`。
